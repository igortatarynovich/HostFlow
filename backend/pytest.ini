[pytest]
# Only repo root on path: `import backend.*` works and the pip `alembic` library
# is not shadowed by this repo's `backend/alembic/` migrations directory.
pythonpath = ..
testpaths = tests
asyncio_mode = auto
markers =
    postgres_integration: full app + Postgres (tenant isolation, lifespan); run in CI with services.db
