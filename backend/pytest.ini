[pytest]
pythonpath = . ..
testpaths = tests
asyncio_mode = auto
# Block real SMTP for RODO/invites when pytest loads this ini (overridden again in tests/conftest.py).
env =
    EMAIL_DELIVERY_MODE=mock
    HOSTFLOW_TEST_LIGHT_STARTUP=1
