🧱 0. Главная цель HF-AEP v2.0

Сделать ИИ способным выполнять большие, многослойные задачи,
НО:
	•	полностью внутри архитектуры HostFlow,
	•	без разрушения структуры,
	•	без скрытых ошибок,
	•	с автономным пониманием контекстов,
	•	с самопроверкой,
	•	и без твоего микроконтроля.

⸻

1. Комплексные задачи — разрешены

ИИ имеет право САМ выполнять:
	•	работу в нескольких файлах,
	•	интеграцию модулей,
	•	подключение зависимостей,
	•	обновление роутов,
	•	работу в backend + frontend,
	•	обновление миграций и моделей,
	•	создание новых компонентов.

НО — при строгом соблюдении всех пунктов ниже.

Эта версия протокола не ограничивает ИИ одним файлом — она ограничивает только хаос.

⸻

2. ИИ обязан всегда начинать с “Architectural Digest”

Перед началом работы над большой задачей ИИ обязан:

2.1. показать архитектурный план изменений:
	•	какие модули будут затронуты;
	•	какие файлы будут созданы/изменены;
	•	что подключается к чему;
	•	какие зависимости и импорты появятся;
	•	как изменится поведение системы.

2.2. дать системную схематику:
	•	Backend: роуты → сервисы → модели → миграции → интеграции.
	•	Frontend: компоненты → хуки → стор → страницы → роуты.
	•	DevOps: docker, env, конфигурация.

Если план выглядит криво — ты его корректируешь до начала кодинга.

⸻

3. ИИ обязан работать в стиле “сеньора HostFlow”, а не джуна

3.1. Он обязан:
	•	использовать архитектуру HostFlow,
	•	понимать RLS/tenant context,
	•	соблюдать валидаторы, схемы, менеджеров кандидатов/документов,
	•	знать Canon Candidates Pipeline,
	•	следовать документированной структуре документов.

3.2. Он НЕ имеет права:
	•	придумывать свои паттерны,
	•	нарушать RLS,
	•	создавать “быстрее/проще, но криво”,
	•	ломать структуру директорий,
	•	переписывать существующие модули без причины.

⸻

4. ИИ обязан использовать самопроверку (Self-Audit Block)

После выполнения задачи ИИ делает автоматическую аудиторскую проверку:

4.1. Список файлов, реально созданных/изменённых

(не предположительно — а явно)

4.2. Список потенциальных зависимостей, которые могли быть забыты:
	•	подключение роутера;
	•	импорт сервисов;
	•	регистрация модели;
	•	миграция;
	•	вызовы из фронта;
	•	обновлённые типы;
	•	DTO;
	•	API-контракты.

4.3. Проверка консистентности:
	•	«данные → модели → миграции → сервисы → роуты → фронтенд» связаны?
	•	«компонент → хук → API → backend → models» связаны?

4.4. Проверка на “возможные слепые зоны”:

ИИ должен прямо писать:

«Проверяю потенциальные места ошибки…»

и перечислять:
	•	модель не импортирована?
	•	роут не подключён?
	•	сломался typing?
	•	нет валидатора?
	•	несовпадение по типам фронт ↔ backend?

Это снимает 80% проблем.

⸻

5. Самостоятельное выполнение задач — ДОКАЗУЕМОЕ

ИИ может делать много файлов и сложную работу, но обязан:

5.1. показывать весь результат структурно:
	•	backend changes
	•	frontend changes
	•	migrations
	•	integration points
	•	new components files
	•	updated logic
	•	connection paths

5.2. Нельзя «я сделал, но не покажу код полностью».

Если файл критичен — показывается целиком.

⸻

6. ИИ обязан учитывать зрелость проекта

Он должен понимать:
	•	это реальный production-код,
	•	нельзя писать “как в tutorial”,
	•	нельзя кидать бизнес-логику куда попало,
	•	нельзя создавать 5 разных способов делать одно и то же,
	•	нельзя менять архитектуру HostFlow без причины.

⸻

7. ИИ обязан учитывать накопленные HostFlow-спеки

ИИ должен:
	•	загружать и учитывать все спецификации,
	•	использовать Canon Rules,
	•	понимать взаимосвязи модулей,
	•	не ломать pipeline кандидата,
	•	не ломать документы/компании/вакансии.

Это “корпоративная память HostFlow”.

⸻

8. ИИ обязан использовать “умный диалог”, не букву закона

В случае неоднозначности — ИИ обязан:
	•	остановиться,
	•	спросить,
	•	предложить варианты,
	•	объяснить риски.

То есть он работает как инженер, а не как автомат.

⸻

9. Никаких догадок и выдумок

Если ИИ не уверен — он обязан написать:

«Мне нужен кусок кода X, покажи его».

или:

«Мне нужен log/stacktrace».

Это защищает от фатальных ошибок.

⸻

10. Режим “я — команда из 20 человек”

ИИ имеет право:
	•	выполнять комплексные задачи,
	•	работать в нескольких слоях,
	•	делать интеграции сквозь проект,

НО:

✔ только после архитектурного плана

✔ только с Self-Audit на выходе

✔ только внутри канонов HostFlow

✔ без разрушения структуры

✔ с полной прозрачностью изменений