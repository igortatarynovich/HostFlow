# Закрытие инженерного контура фаз 2–8 → ручные тесты

**Дата фиксации:** 2026-04-20  
**Следующий гейт:** ручная приёмка по `docs/specs/journeys/README.md` и UAT §2.2.C–J в `HOSTFLOW_AUDIT_AND_PLAN.md`.

Этот документ задаёт **однозначную трактовку**: что считается **закрытым инженерно** (код/спеки доставлены), что переходит **только в ручной QA**, а что **осознанно откладывается** в бэклог после стабилизации (wave 2), без блокировки релиз-кандидата.

---

## Фаза 2 — Production Launch Readiness

| Пункт | Инженерно | Дальше |
|-------|-------------|--------|
| **2.1.A** Канон `plans-matrix.md` | **Да** — файл существует, связи с кодом описаны. Уточнения Enterprise/Trial и др. — **2.1.C–J**, бэклог. | Синхронизация кода ↔ матрица — **2.1.B** (тест), остальное — wave 2. |
| **2.1.B–J** | Частично / открыто | Wave 2 (биллинг UI, trial caps, enterprise first-class, …). |
| **2.2.A** Персоны | **Да** — `docs/specs/personas.md`. | — |
| **2.2.B** Journeys | **Да** — `docs/specs/journeys/*`. | — |
| **2.2.C–J** UAT-прогоны | Нет (не автоматизируются) | **Только ручные тесты** по чеклистам. |
| **2.3.C** Giant pages | Нет | Wave 2 (декомпозиция после UAT). |
| **2.5.G1–G4, G6–G10** (кроме оговоренных) | **Да** — см. тело `HOSTFLOW_AUDIT_AND_PLAN.md`. | — |
| **2.5.G5** manager→recruiter | Stages A–F **да**; Stage G (destructive DB) | **Wave 2** после стабильности shadow-write в prod (как в спеке). |
| **2.5.G6 stage 2** панели Work Hub | **Да** — MyTasks, TodayPlanner, RiskDigest, ManagerLoad, calendar focus, `is_solo_admin`. | **UAT** «вижу план дня» — **ручной** §2.2.C–H. |
| **2.5.G8 stage 2** Next-action | **Да** — lead, vacancy, document, thread. | — |
| **2.6** Доменные спеки | **Да** — tenant-types, own-company, lead-types, vacancy-statuses (+ часть execution в коде). | Оставшиеся стадии из spec — wave 2 по приоритету. |

---

## Фазы 3–7 — модульный hardening (целевой UX §IV)

Полный чеклист чекбоксов в `HOSTFLOW_AUDIT_AND_PLAN.md` (реестр документов, единая сущность «задача», unified inbox, Stripe §2.18, портал «продакт») — это **дорожная карта продукта**, а не условие «всё зелёное» для первого RC. OCR-трек по документам зафиксирован как out of scope.

**Инженерное закрытие для перехода к ручным тестам:** текущие модули (документы, календарь/задачи, inbox, биллинг, портал) **работают в продукте**; расхождения с идеальным §IV фиксируются как **бэклог wave 2** после UAT.

---

## Фаза 8 — поиск и аналитика

См. `docs/specs/phase-8-roadmap.md`.

- **8.1** глобальный поиск документов + кандидат — **инженерно закрыт**.  
- **8.3** воронка — база + UI custom cohort / WoW — **инженерно закрыт**; сохранённые пресеты срезов — wave 2.  
- **8.2, 8.4, 8.5** — wave 2 / опционально.

---

## Что делают люди дальше

1. Пройти **UAT 2.2.C–H** (и при необходимости I–J) по `docs/specs/journeys/`.  
2. Зафиксировать баги отдельными задачами.  
3. После стабилизации — подтягивать **wave 2** из таблиц выше (планы 2.1, G5 Stage G, §IV, фаза 6 Stripe, фаза 8 epics).
