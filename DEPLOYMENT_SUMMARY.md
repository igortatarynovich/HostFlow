# Сводка деплоя исправлений видимости оверлея

## Дата: 28 ноября 2024, 15:53

## Выполненные исправления

### 1. Увеличение видимости оверлея детекции
- **Z-index**: Увеличен до `9999` (было `100`)
- **Толщина обводки**: Увеличена до `20px` (было `8px`)
- **Размер угловых маркеров**: Увеличен до `20px` радиус (было `15px`)
- **Тени и эффекты**: Добавлены для лучшей видимости

### 2. Улучшение валидации детекции
- **Покрытие кадра**: Отклоняются квады, заполняющие > 85% кадра (было 98%)
- **Проверка краев**: Отклоняются квады слишком близко к краям (5% отступ)

### 3. Деплой
- Файлы успешно скопированы в `/var/www/hostflow-frontend/`
- Новый файл: `index-B_mS4J3Q.js` (1.5M)
- Владелец: `www-data:www-data`

## Что нужно сделать

### ОБЯЗАТЕЛЬНО: Очистить кеш браузера
После деплоя браузер может использовать старую версию из кеша. Необходимо:

1. **Очистить кеш браузера**:
   - Chrome/Edge: `Ctrl+Shift+Delete` (Windows) или `Cmd+Shift+Delete` (Mac)
   - Выберите "Изображения и файлы в кеше"
   - Или используйте режим инкогнито

2. **Жесткая перезагрузка страницы**:
   - Windows/Linux: `Ctrl+Shift+R` или `Ctrl+F5`
   - Mac: `Cmd+Shift+R`

### Проверка работы

1. Откройте страницу сканирования
2. Откройте консоль разработчика (F12)
3. Проверьте логи:
   - `[ScanningOverlay] ✅ Creating detectedPath` - оверлей создается
   - `[scanner] Document detected` - документ обнаружен
4. Визуально проверьте:
   - **Толстая обводка** (20px) вокруг документа
   - **Большие угловые маркеры** (20px радиус)
   - **Цвет обводки**: зеленый (готово), желтый (почти готово), красный (не найден)

## Ожидаемое поведение

- **Документ не найден**: Красная пунктирная рамка-подсказка
- **Документ найден, но не готов**: Желтая обводка с пунктиром
- **Документ готов к съемке**: Зеленая сплошная обводка

## Если проблема сохраняется

1. Проверьте консоль браузера на наличие ошибок
2. Проверьте, что используется правильный файл: откройте Network tab и проверьте, какой `index-*.js` загружается
3. Убедитесь, что кеш очищен (используйте режим инкогнито)

## Технические детали

- **Файл**: `/var/www/hostflow-frontend/assets/index-B_mS4J3Q.js`
- **Размер**: 1.5M
- **Владелец**: `www-data:www-data`
- **Время деплоя**: 28 ноября 2024, 15:53

