Landing page visuals (CrmLandingPage).
Replace these SVG stubs with real PNG/WebP exports from the product, then update paths in:
  hostflow-frontend/src/i18n/en.json, ru.json, pl.json
under public.crm_landing.hero.screenshot_src and how.steps.*.screenshot_src
(e.g. /landing/hero.webp). Use absolute paths from site root (public/).
