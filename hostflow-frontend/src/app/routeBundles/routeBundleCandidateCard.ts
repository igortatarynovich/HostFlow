/** Single heavy page — own chunk so build doesn’t merge with Candidates.tsx graph. */
export { default as CandidateCard } from '../../pages/CandidateCard'
export { default as CandidateRequirementsWorkspace } from '../../pages/candidate/CandidateRequirementsWorkspace'
